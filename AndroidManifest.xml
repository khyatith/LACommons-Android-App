<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.slidingmenu"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault" >
        <activity
            android:name="info.androidhive.slidingmenu.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.androidhive.slidingmenu.TrekDetails" ></activity>
        <activity
            android:name="info.androidhive.slidingmenu.TrekDescription" ></activity>
        <activity
            android:name="info.androidhive.slidingmenu.LoginActivity" ></activity>
        <activity
            android:name="info.androidhive.slidingmenu.SignUpActivity" ></activity>
        <activity
            android:name="info.androidhive.slidingmenu.ShoppingCartActivity" ></activity>
    </application>

</manifest>
